---
description: Plan usage for Claude and Codex, side by side
---

Run `codex-bridge usage` and show the output verbatim.

If it reports Claude usage as "not seen yet", explain that Anthropic sends plan
usage on answered requests rather than on demand, so it appears after the first
Claude model call through the gateway.
